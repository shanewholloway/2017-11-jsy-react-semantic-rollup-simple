const path = require('path')
const express = require('express')
const expressStaticGzip = require('express-static-gzip')

export const api = express()

api.use @ "/", expressStaticGzip @ path.join @ __dirname, '../public'

